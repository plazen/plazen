# Plazen Development Mode Environment Configuration
# NOTE: THIS IS A FULL VERSION, NO VALUES NEED TO BE CHANGED

# P.S. use `make dev` to start the server instantly

# Copy this file to .env to run Plazen without a database

# Enable dev mode - uses in-memory storage and mock authentication
DEV_MODE=true
NEXT_PUBLIC_DEV_MODE=true

# Base URL for local development
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Encryption key (required even in dev mode for data consistency)
# This is a dummy key for development only - DO NOT use in production
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# These are placeholder values - not used in dev mode but needed to prevent startup errors
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=dev-anon-key
SUPABASE_SERVICE_ROLE_KEY=dev-service-role-key
NEXT_PUBLIC_SUPABASE_AVATAR_BUCKET=avatars

# For Prisma client generation
DATABASE_URL="mock_db_totally_legit"
